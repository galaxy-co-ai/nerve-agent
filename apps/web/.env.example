# =============================================================================
# NERVE AGENT Environment Variables
# =============================================================================
# Copy this file to .env.local for local development
# In production, set these in your hosting provider (Vercel, etc.)

# =============================================================================
# DATABASE (Required)
# =============================================================================
# Get these from https://console.neon.tech
# DATABASE_URL uses the pooled connection (for serverless)
# DIRECT_URL uses direct connection (for Prisma migrations)

DATABASE_URL=postgresql://user:password@host-pooler.region.aws.neon.tech/dbname?sslmode=require
DIRECT_URL=postgresql://user:password@host.region.aws.neon.tech/dbname?sslmode=require

# =============================================================================
# AI PROVIDERS
# =============================================================================

# ANTHROPIC (Required) - Agent chat, complex reasoning, tool use
# Get from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-xxx

# OPENAI (Recommended) - Embeddings, transcription, fallback generation
# Get from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxx

# MISTRAL (Optional) - Fast summaries, quick drafts, standup notes
# Get from https://console.mistral.ai/api-keys
MISTRAL_API_KEY=xxx

# PERPLEXITY (Optional) - Web search augmented research
# Get from https://perplexity.ai/settings/api
PERPLEXITY_API_KEY=pplx-xxx

# FIRECRAWL (Optional) - Web scraping, URL to markdown
# Get from https://firecrawl.dev/account
FIRECRAWL_API_KEY=fc-xxx

# =============================================================================
# CLERK AUTHENTICATION (Required for Production)
# =============================================================================
# Get from https://dashboard.clerk.com
# Note: Auth is bypassed in dev mode (see lib/auth.ts)

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx

# Clerk redirect URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/dashboard
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/dashboard

# =============================================================================
# EMAIL - RESEND (Optional)
# =============================================================================
# Get from https://resend.com/api-keys
# Used for: Portal magic links, notifications
# If not set, magic links will be displayed directly (dev mode)

# RESEND_API_KEY=re_xxx
# RESEND_FROM_EMAIL=noreply@yourdomain.com

# =============================================================================
# APP CONFIGURATION (Optional)
# =============================================================================
# Public URL of your app (used for generating links)
# Defaults to request origin if not set

# NEXT_PUBLIC_APP_URL=https://nerve-agent.yourdomain.com

# =============================================================================
# PUSHER - Real-time Communication (Desktop App)
# =============================================================================
# Get from https://dashboard.pusher.com/apps
# Used for: Desktop app ↔ Web app real-time communication

PUSHER_APP_ID=xxx
PUSHER_SECRET=xxx
NEXT_PUBLIC_PUSHER_KEY=xxx
NEXT_PUBLIC_PUSHER_CLUSTER=us2

# =============================================================================
# CRON / SCHEDULED JOBS (Production)
# =============================================================================
# Secret to secure cron endpoints (Vercel adds this as Authorization header)
# Generate with one of these commands:
#   Linux/Mac: openssl rand -base64 32
#   Windows:   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#   Or use:    node -e "console.log(crypto.randomUUID() + '-' + Date.now())"

CRON_SECRET=xxx
